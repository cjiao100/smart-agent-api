# Smart Agent API

<div align="center">

一个基于 FastAPI 和 LangChain 的智能多任务问答助手 API

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-green.svg)](https://fastapi.tiangolo.com/)
[![LangChain](https://img.shields.io/badge/LangChain-Latest-orange.svg)](https://langchain.com/)

</div>

## 📖 简介

Smart Agent API 是一个智能对话系统,能够理解用户的自然语言问题,智能判断是否需要调用外部工具（如天气查询、新闻获取）,并返回结构化的答案。支持多轮对话、上下文追问和会话历史管理。

### ✨ 核心特性

- 🤖 **智能路由** - 基于 LLM 和规则的双重路由机制,智能判断工具调用
- 🔧 **工具集成** - 支持天气查询、新闻获取等多种工具,可扩展的工具注册系统
- 💬 **多轮对话** - 完整的会话状态管理,支持上下文追问
- 🔄 **防护机制** - 工具调用次数限制,避免无限递归
- 📊 **灵活输出** - 支持 JSON 和文本两种输出格式
- 🚀 **高性能** - 基于 FastAPI 的异步架构

### 🛠️ 技术栈

- **Web 框架**: FastAPI
- **AI 编排**: LangChain
- **LLM 服务**: OpenRouter (支持多种模型)
- **外部 API**: 和风天气、天行数据
- **状态管理**: 内存存储 (支持扩展至 Redis)

## 📁 项目结构

\`\`\`
smart-agent-api/
├── agents/              # Agent 编排层
│   ├── agent.py        # Agent 核心逻辑
│   └── route.py        # 路由决策
├── api/                 # API 接口层
│   └── main.py         # FastAPI 应用
├── config/              # 配置管理
│   └── settings.py     # 环境变量和配置
├── schemas/             # 数据模型
│   ├── chat.py         # 聊天相关模型
│   └── tool.py         # 工具相关模型
├── state/               # 状态管理
│   └── store.py        # 会话状态存储
├── tools/               # 工具层
│   ├── registry.py     # 工具注册表
│   ├── weathor_tool.py # 天气查询工具
│   ├── news_tool.py    # 新闻获取工具
│   └── lc_tools.py     # LangChain 工具适配
├── utils/               # 工具类
│   └── logger.py       # 日志工具
├── logs/                # 日志目录
├── main.py              # 应用入口
├── requirements.txt     # 依赖列表
├── .env.example         # 环境变量模板
├── .gitignore          # Git 忽略配置
└── DEPLOYMENT.md        # 部署指南
\`\`\`

## 🚀 快速开始

### 前置要求

- Python 3.11 或更高版本
- pip 包管理器

### 安装步骤

1. **克隆项目**

\`\`\`bash
git clone https://github.com/cjiao100/smart-agent-api.git
cd smart-agent-api
\`\`\`

2. **安装依赖**

\`\`\`bash
pip install -r requirements.txt
\`\`\`

3. **配置环境变量**

\`\`\`bash
# 复制环境变量模板
cp .env.example .env

# 编辑 .env 文件,填写你的 API 密钥
# 需要注册以下服务获取密钥:
# - 和风天气: https://dev.qweather.com/
# - 天行数据: https://www.tianapi.com/
# - OpenRouter: https://openrouter.ai/
\`\`\`

4. **运行项目**

\`\`\`bash
python main.py
\`\`\`

服务将在 \`http://localhost:8000\` 启动。访问 \`http://localhost:8000/docs\` 查看 API 文档。

## ⚙️ 环境配置

### 必需的环境变量

| 变量名 | 说明 | 获取方式 |
|--------|------|----------|
| \`QWEATHER_API_KEY\` | 和风天气 API 密钥 | [注册获取](https://dev.qweather.com/) |
| \`QWEATHER_BASE_URL\` | 和风天气 API 地址 | 默认: \`https://devapi.qweather.com/v7\` |
| \`TIAN_API_KEY\` | 天行数据 API 密钥 | [注册获取](https://www.tianapi.com/) |
| \`TIAN_API_BASE_URL\` | 天行数据 API 地址 | 默认: \`https://api.tianapi.com\` |
| \`OPENROUTER_API_KEY\` | OpenRouter API 密钥 | [注册获取](https://openrouter.ai/) |
| \`OPENROUTER_BASE_URL\` | OpenRouter API 地址 | 默认: \`https://openrouter.ai/api/v1\` |

### 可选的环境变量

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| \`APP_NAME\` | 应用名称 | \`MultiTaskQAAssistant\` |
| \`APP_VERSION\` | 应用版本 | \`1.0.0\` |
| \`LOG_LEVEL\` | 日志级别 (DEBUG/INFO/WARNING/ERROR) | \`INFO\` |
| \`MAX_CONVERSATION_HISTORY\` | 最大对话历史记录数 | \`50\` |
| \`CACHE_TTL\` | 缓存过期时间(秒) | \`3600\` |

详细的环境变量说明请参考 [.env.example](.env.example) 文件。

## 📡 API 接口

### 1. 聊天接口

**POST** \`/chat\`

发送消息并获取 AI 回复。

**请求参数:**

\`\`\`json
{
  "session_id": "demo-session",
  "message": "北京今天天气怎么样?",
  "output_format": "text"  // 可选: "text" 或 "json"
}
\`\`\`

**响应示例:**

\`\`\`json
{
  "session_id": "demo-session",
  "answer": "北京今天天气晴朗,温度 25°C,适合出行。",
  "tool_used": {
    "tool_name": "weather",
    "parameters": {"city": "北京"}
  },
  "state": {
    "messages": [...],
    "last_tool": {...},
    "tool_calls": 1
  }
}
\`\`\`

### 2. 历史记录接口

**GET** \`/history/{session_id}\`

获取指定会话的聊天历史。

**响应示例:**

\`\`\`json
{
  "session_id": "demo-session",
  "messages": [
    {
      "role": "user",
      "content": "北京今天天气怎么样?"
    },
    {
      "role": "assistant",
      "content": "北京今天天气晴朗,温度 25°C。"
    }
  ]
}
\`\`\`

### API 文档

启动服务后,访问以下地址查看完整的 API 文档:

- **Swagger UI**: \`http://localhost:8000/docs\`
- **ReDoc**: \`http://localhost:8000/redoc\`

## 💡 使用示例

### 天气查询

\`\`\`bash
curl -X POST "http://localhost:8000/chat" \\
  -H "Content-Type: application/json" \\
  -d '{
    "session_id": "user123",
    "message": "上海明天天气如何?",
    "output_format": "text"
  }'
\`\`\`

### 新闻查询

\`\`\`bash
curl -X POST "http://localhost:8000/chat" \\
  -H "Content-Type: application/json" \\
  -d '{
    "session_id": "user123",
    "message": "最近有什么科技新闻?",
    "output_format": "json"
  }'
\`\`\`

### 上下文追问

\`\`\`bash
# 第一轮
curl -X POST "http://localhost:8000/chat" \\
  -H "Content-Type: application/json" \\
  -d '{"session_id": "user123", "message": "深圳天气怎么样?"}'

# 第二轮 - 基于上下文追问
curl -X POST "http://localhost:8000/chat" \\
  -H "Content-Type: application/json" \\
  -d '{"session_id": "user123", "message": "那明天呢?"}'
\`\`\`

## 🏗️ 系统架构

### 架构设计

\`\`\`
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────┐
│         FastAPI (API Layer)         │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│    Agent Router (Orchestration)     │
│  ┌─────────────┐  ┌──────────────┐ │
│  │ Rule-based  │  │  LLM-based   │ │
│  │   Router    │  │    Router    │ │
│  └─────────────┘  └──────────────┘ │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│        Tool Registry & Execution    │
│  ┌─────────┐ ┌─────────┐ ┌────────┐│
│  │ Weather │ │  News   │ │  ...   ││
│  │  Tool   │ │  Tool   │ │  Tool  ││
│  └─────────┘ └─────────┘ └────────┘│
└─────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│      State Management (Memory)      │
└─────────────────────────────────────┘
\`\`\`

### 核心组件

- **API Layer**: FastAPI 接口层,处理 HTTP 请求
- **Agent Router**: 智能路由,决定是否调用工具及调用哪个工具
  - **Rule-based Router**: 基于关键词和规则的路由判断
  - **LLM-based Router**: 基于大语言模型的智能路由
- **Tool Registry**: 工具注册和管理系统
- **State Management**: 会话状态和历史记录管理

### 工作流程

1. 用户通过 \`/chat\` 接口发送消息
2. Agent Router 分析消息,决定是否需要调用工具
3. 如需调用工具,从 Tool Registry 获取并执行对应工具
4. 将工具结果和上下文传递给 LLM 生成自然语言回复
5. 保存会话状态,返回结构化响应

## 🔒 安全机制

- ✅ **调用限制**: 工具调用次数限制,防止无限递归
- ✅ **输入验证**: 严格的请求参数验证
- ✅ **错误处理**: 完善的异常捕获和 fallback 机制
- ✅ **敏感信息保护**: API 密钥通过环境变量管理,不记录到日志
- ✅ **环境隔离**: 开发、测试、生产环境分离

## 🚢 部署

详细的部署指南请参考 [DEPLOYMENT.md](DEPLOYMENT.md),包括:

- 🐳 Docker 部署
- ☸️ Kubernetes 部署
- ☁️ 云平台部署 (AWS, Azure, GCP, Vercel, Railway, Render)
- 🔄 CI/CD 配置 (GitHub Actions)
- 🔐 密钥管理最佳实践

## 🛠️ 开发指南

### 添加新工具

1. 在 \`tools/\` 目录创建新的工具文件
2. 实现工具的核心逻辑
3. 在 \`tools/registry.py\` 中注册工具
4. 在 \`tools/lc_tools.py\` 中添加 LangChain 工具适配

示例:

\`\`\`python
# tools/my_tool.py
def my_tool_handler(param: str) -> dict:
    """工具处理逻辑"""
    return {"result": "..."}

# tools/registry.py
TOOL_REGISTRY = {
    "my_tool": {
        "name": "my_tool",
        "description": "工具描述",
        "handler": my_tool_handler
    }
}
\`\`\`

### 运行测试

\`\`\`bash
# 安装测试依赖
pip install pytest pytest-cov

# 运行测试
pytest

# 生成覆盖率报告
pytest --cov=. --cov-report=html
\`\`\`

### 日志配置

项目使用自定义的日志系统,日志文件保存在 \`logs/\` 目录:

- \`app.log\` - 应用日志
- 日志级别通过 \`LOG_LEVEL\` 环境变量控制

## 📊 性能优化

- 使用 FastAPI 的异步特性提升并发处理能力
- 工具结果缓存机制 (TTL 可配置)
- 会话状态支持扩展至 Redis 以提升扩展性
- 合理的超时和重试策略

## 🗺️ 路线图

- [ ] 支持更多工具 (翻译、计算器、数据库查询等)
- [ ] 实现 Redis 会话存储
- [ ] 添加用户认证和权限管理
- [ ] 支持流式响应 (Server-Sent Events)
- [ ] 添加监控和追踪 (OpenTelemetry)
- [ ] 支持多模态输入 (图片、语音)
- [ ] 实现工具组合和链式调用

## 🤝 贡献

欢迎提交 Issue 和 Pull Request!

### 贡献指南

1. Fork 本仓库
2. 创建特性分支 (\`git checkout -b feature/AmazingFeature\`)
3. 提交更改 (\`git commit -m 'Add some AmazingFeature'\`)
4. 推送到分支 (\`git push origin feature/AmazingFeature\`)
5. 开启 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 详见 LICENSE 文件

## 📞 联系方式

- **作者**: cjiao100
- **Issues**: [GitHub Issues](https://github.com/cjiao100/smart-agent-api/issues)
- **讨论**: [GitHub Discussions](https://github.com/cjiao100/smart-agent-api/discussions)

## 🙏 致谢

感谢以下开源项目:

- [FastAPI](https://fastapi.tiangolo.com/)
- [LangChain](https://langchain.com/)
- [Pydantic](https://pydantic.dev/)
- [OpenRouter](https://openrouter.ai/)

---

<div align="center">
如果觉得这个项目有帮助,请给个 ⭐️ Star 支持一下!
</div>
